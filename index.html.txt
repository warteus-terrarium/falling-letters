<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<title>Pluie de lettres</title>
<style>
  html, body {
    margin: 0;
    padding: 0;
    height: 100%;
    overflow: hidden;
    background: #111; /* couleur de fond */
    font-family: Arial, sans-serif;
  }

  .letter {
    position: absolute;
    top: -50px;
    font-size: 32px;
    font-weight: bold;
    color: #0f0; /* vert type Matrix, modifiable */
    animation: fall linear forwards;
  }

  @keyframes fall {
    to {
      transform: translateY(110vh);
      opacity: 0;
    }
  }
</style>
</head>
<body>

<script>
const text = "HELLO WORLD"; // Modifie ici ton texte
const letters = text.split("");

// Fonction pour créer une lettre
function createLetter(letter) {
  const span = document.createElement("span");
  span.className = "letter";
  span.innerText = letter;

  span.style.left = Math.random() * window.innerWidth + "px";
  span.style.animationDuration = (2 + Math.random() * 3) + "s";

  document.body.appendChild(span);

  setTimeout(() => {
    span.remove(); // Supprime après animation pour garder le DOM léger
  }, 5000);
}

// Pluie continue de lettres
setInterval(() => {
  const randomLetter = letters[Math.floor(Math.random() * letters.length)];
  createLetter(randomLetter);
}, 150);
</script>

</body>
</html>